<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".NFCActivity">

    <TextView
        android:text="NFC Status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/nfcTextStatus"
        android:layout_marginTop="83dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sendView"
        android:layout_below="@+id/messageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="41dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Write your message for NFC in the field below"
        android:id="@+id/messageView"
        android:layout_below="@+id/nfcTextStatus"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="143dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Switch On"
        android:id="@+id/switchOnBtn"
        android:layout_below="@+id/nfcTextStatus"
        android:layout_alignLeft="@+id/messageView"
        android:layout_alignStart="@+id/messageView"
        android:layout_marginTop="43dp"
        android:layout_toLeftOf="@+id/sendView"
        android:layout_toStartOf="@+id/sendView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Switch Off"
        android:id="@+id/switchOffBtn"
        android:layout_alignTop="@+id/switchOnBtn"
        android:layout_alignRight="@+id/messageView"
        android:layout_alignEnd="@+id/messageView"
        android:layout_toRightOf="@+id/sendView"
        android:layout_toEndOf="@+id/sendView" />

</RelativeLayout>
